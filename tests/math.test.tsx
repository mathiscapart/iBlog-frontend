import { describe, test, expect } from "vitest"

describe("Mathematic Operations", () => {
    test("should add 2 numbers", () => {
        expect(1+1).toBe(2);
    });

    test("should return the same numbers", () => {
        expect(1).toBe(1)
    })

})